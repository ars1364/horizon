name: Build & Push Horizon Image

on:
  push:
    branches: [master]
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      kolla_base:
        description: "Override Kolla base image (leave blank for default quay.io image)"
        required: false
        default: ""

env:
  IMAGE: ghcr.io/ars1364/horizon
  DEFAULT_BASE: quay.io/openstack.kolla/ubuntu-source-horizon:2025.1-ubuntu-noble

jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Resolve base image
        id: base
        run: |
          INPUT="${{ github.event.inputs.kolla_base }}"
          echo "image=${INPUT:-${{ env.DEFAULT_BASE }}}" >> "$GITHUB_OUTPUT"

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.kolla
          push: true
          tags: |
            ${{ env.IMAGE }}:2025.1
            ${{ env.IMAGE }}:latest
          build-args: |
            KOLLA_BASE=${{ steps.base.outputs.image }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
